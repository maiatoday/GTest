apply plugin: "cpp"

executables{
    main{}
}

sources {
    main {
        // Configure an existing CppSourceSet
        cpp {
            source {
                //srcDirs "src/main/cpp", "src/shared/c++"
                //include "**{@literal /}*.cpp"
            }
            exportedHeaders {
               // srcDirs "src/main/include", "src/shared/include"
            }
        }
    }
}

binaries.all {
	//Preprocessor macros go here
	//cppCompiler.define "TEST_DEFINE"

	// toolchain args go here
	if (toolChain in Gcc) {
		cppCompiler.args "-I/usr/include/gtest"
		linker.args "-lgtest",  "-lpthread"
	}


}